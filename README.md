# personal-finance-manager-application
Windows desktop finance tracker in C++ using linked list, BST, DFS/BFS traversal, and undo/redo stacks — built using main Data Structures known as a direct implementation to data structure
